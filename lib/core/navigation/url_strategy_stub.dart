/// Non-web platforms: there is no browser URL bar, so there is nothing to
/// configure. This stub is selected by the conditional import in main.dart
/// when `dart.library.js_interop` is unavailable (Android / iOS).
void configureUrlStrategy() {}
